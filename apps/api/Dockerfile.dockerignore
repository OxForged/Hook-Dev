# Allowlist for `docker build -f apps/api/Dockerfile .` (context = repo root).
# Everything is excluded, then only what the image needs is let back in.
*

!packages/sdk/package.json
!packages/sdk/package-lock.json
!packages/sdk/tsconfig*.json
!packages/sdk/schema.graphql
!packages/sdk/src/**

!apps/api/package.json
!apps/api/package-lock.json
!apps/api/tsconfig.json
!apps/api/tsconfig.build.json
!apps/api/prisma/**
!apps/api/src/**
!apps/api/config/**

# Belt and braces: never, even if a pattern above is widened later.
**/.env
**/.env.*
**/*.key
**/*.pem
**/node_modules
**/dist
