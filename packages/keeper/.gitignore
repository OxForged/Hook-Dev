node_modules/
dist/
keeper.config.json
.env
.env.*
!.env.example

# Deploy-time secret. Read by docker-compose; never committed.
.env

# Output of latch-feed-watch (read-only staleness logs). Local measurement data, not source.
logs/
*.jsonl
